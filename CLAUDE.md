# 作業メモ

## 公開先

| URL | 中身 | 更新のされ方 |
| --- | --- | --- |
| https://ruke2.github.io/Claude-/dist/skyline.html | 単一HTML（ビルド成果物） | `node build.mjs` を実行してから push |
| https://ruke2.github.io/Claude-/ | `index.html` + `src/`（ES Modules） | push すればそのまま反映 |
| https://claude.ai/artifact/LBZmeyJ3xHNwsZ3v8K8LSj | 単一HTML（Artifact） | `dist/artifact.html` を publish |

GitHub Pages の設定は「Deploy from a branch」／ブランチ `claude/developer-game-v1-l75etn` ／ `/ (root)`。
プッシュの1〜2分後に自動デプロイされる。

## プッシュ前に必ずやること

```bash
node build.mjs     # src/ styles/ index.html → dist/skyline.html と dist/artifact.html
                   # あわせて dist/manifest.webmanifest, dist/sw.js, dist/icons/ も配る
```

**これを忘れると `dist/skyline.html` が古いままになり、公開ページに変更が反映されない。**
`src/` や `styles/main.css`、`index.html` を触ったら必ず実行する。

アイコンを描き直したときだけ `node scripts/make-icons.mjs` を先に実行する。

## 構成

- `index.html` + `src/**` — 開発用のソース（ES Modules、HTTP経由でのみ動く）
- `styles/main.css` — UI スタイル（白基調）
- `build.mjs` — esbuild で1ファイルに束ねる
- `dist/skyline.html` — サーバー不要で開ける単一HTML
- `dist/artifact.html` — Artifact 公開用（外側のタグなし）
- `manifest.webmanifest` / `sw.js` / `assets/icons/` — ホーム画面に追加して遊ぶための一式
- `scripts/make-icons.mjs` — アイコンPNGの生成（zlibだけで書き出す）

## ゲームの基本設計

- 内部の金額単位は**百万円**。表示は `src/core/format.js` の `money()` で億円・兆円に変換する
- 1ターン = 1週。13週で四半期決算。カレンダーは `src/core/time.js`
- 乱数はシード付き（`src/core/rng.js`）。`g.rngState` を毎ターン更新する
- セーブは `src/core/save.js`（localStorage、4スロット＋オートセーブの退避1つ）。
  `pendingReport` など一時データは保存しない

## セーブを壊さないための決めごと

**`src/core/save.js` の `PREFIX` と `META` は絶対に変えない。**
変えるとユーザーのセーブが二度と見つからなくなる。

- ゲームの状態に項目を足すのは自由。読み込み時に `migrate()` が
  新しいゲームをひな型にして、足りない項目を補う（既存の値には触らない）
- 区画（`g.cells`）は同じ位置どうしで突き合わせる。1つのひな型でまとめて埋めると
  道路や海に敷地面積が付いてしまう
- **オブジェクトの配列も、ひな型の先頭だけで埋めないこと。**
  `fill()` はIDで突き合わせ、無ければ同じ位置の要素を使う。
  以前ここが `sv[0]` 固定だったため、競合各社に平均年収と地盤を足したとき、
  読み込んだセーブでは8社すべてが1社目（四井不動産）と同じ値になっていた。
  要素ごとに中身が違う配列（`rivals` / `brands`）に項目を足したら、
  **必ず古いセーブを読み込んで各要素の値が別々になっているか確かめる**
- **地図を広げるときは、`MAP_ROWS` の左上 24×24 を絶対に動かさないこと。**
  右側と下側に足していく。区画数が変わると `remapCells()` がIDで突き合わせて、
  地形と地区が一致する区画だけ古いものを引き継ぐ。左上を動かすと引き継げなくなる
- ID（`uid()`）は読み込み時に `syncUid()` で振り直す。これを外すと、
  続きから始めたときに新しい案件が既存の案件と同じIDになる
- 保存する形を変えたときは `SAVE_VERSION` を上げ、`migrate()` に処理を足す

## 竣工処理でいちばん壊れやすいところ

`devPlan()` と `devPlanStack()` は、**同じ名前の値を返さないと竣工時に破綻する**。

必須：`salePrice`（分譲の坪単価）／`rent`（賃貸の募集賃料）／
`saleCostShare`（総事業費を分譲在庫と保有資産に割り振る比率）／`leaseUse`（賃貸部分の主用途）

- `salePrice` が抜けると在庫の `totalValue` が0になり、
  **売上0のまま原価が満額計上される**。竣工のたびに数百億円の営業損失が出る
- `rent` が抜けると保有資産の募集賃料が0になり、簿価だけ残って永久に無収入になる
- `saleCostShare` が実態と合わないと、分譲と賃貸の原価配分がずれる
- 新しい用途や計画関数を足したら、必ず `node scripts/check-lifecycle.mjs` を流す

保有資産の「市場賃料」（`marketRentRaw()`）は、グレードやブランドを含まない素の相場である。
物件ごとの上振れ・下振れは `a.rentIndex` が持つ。
ここを混ぜると、高級物件が永久に「市場比+100%」と判定されて空室が増える。

## 用途の勝ち負けを決めているところ

地区ごとに「本命の用途」が1つ決まるのは、次の3つの掛け算で成り立っている。
どれかを触ったら `node scripts/check-districts.mjs` と勝ち用途カウントを必ず流す。

- `src/sim/valuation.js` の `FIT_MUL(fit)` … 地区適合を収益倍率に変える。
  現在 `0.26 + fit * 0.80`（適合1.00→1.06倍、0.50→0.66倍、0.10→0.34倍）。
  **ここの幅を狭めると、素の賃料がいちばん高い用途がどの地区でも勝つ。**
  以前 `0.74 + fit * 0.28` だった頃は、オフィスと商業が全地区で一度も勝てなかった
- `DISTRICTS[*].rent*` … 素の相場賃料。ホテルは全地区で最も高く設定してあるので、
  地区適合で抑えないと必ずホテルが勝つ
- `src/sim/market.js` の `demand` の位相と振幅 … ホテルは振幅がいちばん大きい（0.34）。
  **位相は他の用途とそろえること。** 逆相にすると、オフィスが底を打つ不況期に
  ホテルだけが突出して「どの地区でもホテル」になる

地区の `landPrice` は、本命用途の事業利益率が2割前後になるように決めている。
数値を動かしたら `scratchpad` の二分探索で引き直すとよい。

## 賃料・分譲単価・建設費の物差しをそろえる

**収入も費用も、必ず同じ `g.market.priceIdx` の上に乗せること。**

| どこ | 何を使っているか |
| --- | --- |
| `devPlan` の分譲単価（`unitPrice`） | `priceIdx` |
| `devPlan` / `devPlanStack` の募集賃料（`rent`） | `priceIdx` |
| 保有物件の市場賃料（`marketRentRaw`） | `priceIdx` |
| 建設費（`buildCost`） | `costIdx` |
| 地価（`landAppraisal`） | `priceIdx` |

以前 `devPlan` の賃料にだけ `priceIdx` が掛かっていなかった。
そのため長く遊ぶほど、建設費と地価だけが上がって賃貸の収入が据え置かれ、
**オフィス・賃貸・物流・商業を本命とする地区の事業利益率が
30年で 32% → 5%、若葉町や城東では赤字にまで落ちていた。**
分譲が本命の地区（汐見・汐凪）だけは単価に `priceIdx` が乗っていたので伸び続け、
「賃貸系の地区だけ儲からなくなる」という形で表に出ていた。

確かめ方は `scratchpad` の長期運転。10年ごとに本命用途の事業利益率を出し、
**どの年代でもおおむね同じ帯に収まっていること**を見る。

## 市況の指数

`priceIdx` と `costIdx` は必ず連動させる。
建設費の目標値は `src/sim/market.js` の `costEquilibrium()` が持っており、
実質トレンド × 価格指数 × 好況局面の資材高 の積になっている。
**片道で増える項だけにすると、長く遊ぶほど建設費が価格から離れて、
どの用途でも「土地に払える上限（残余法）」がマイナスになる。**
古いセーブは読み込み時に `save.js` の `STEPS` で目標値まで引き戻している。

**好況の資材高を `Math.max(0, sin(...))` で上振れだけ取らないこと。**
半周期ぶんの上振れが平均に残り、建設費指数が価格指数より恒久的に6%ほど高い位置に居座る。
建設費指数は `K_COST` でゆっくりしか動かないので、この片道ぶんがそのまま水準差として積み上がり、
30年で 建設費÷価格 が 1.04 → 1.20 まで開いていた。

## 買収・子会社の効果

`src/data/companies.js` の `effect` に書いたキーは、
`subEffect(g, key)` 経由で必ずどこかが読むこと。
読まれていないキーは「説明文に書いてあるのに何も起きない」状態になる。
現在のキーと読み手：

| キー | 読んでいる場所 |
| --- | --- |
| `costCut` / `speed` | `devPlan` / `devPlanStack` の建設費・工期 |
| `salesPower` / `saleSpeed` | `project.js` の `contractSpeed()` |
| `feeCut` | `valuation.js` の `saleFeeRate()` |
| `landInfo` | `land.js` の `generateListings()` |
| `occupancy` / `feeRate` | `sales.js` の稼働率・`currentNOI()` |
| `exitPremium` | 保有物件の売却価格 |
| `hotelNoi` | `currentNOI()` のホテル補正 |
| `lots` / `brand` | `ma.js` の `acquire()` |

競合各社の平均年収は `rivals.js` の `stepPay()` が四半期ごとに動かす。
基本給のベースアップに、利益率（`payMargin` との比）と市況で振れる賞与が乗る。
**片道で上がるだけにしないこと。** 不況の年はきちんと下がるのが狙いで、
`history` に `pay` を残して推移を折れ線で見せている。

買収後のリスクは `ma.js` の `RISK_WINDOW`（3年）で通算確率を週次に割り戻す。
**週あたりの確率をそのまま156週かけないこと**（通算9割が発火する）。
リスクの影響は「顕在化したとき（`r.fired`）」だけに効かせる。
抱えているだけで統合速度を落とすと、二重に罰することになる。

## 売却情報の出方

`src/sim/land.js` の `SIZE_BANDS` で先に金額の帯を選び、その中から区画を引いている。
区画を一様に引くと、区画数の多い（＝安い）地区に引きずられて大型案件が出なくなる。
地区を足したときは帯の重みを見直し、`scratchpad` の帯テスト（想定地価ごとの出現比率）で確かめる。

- 帯の境目は `pickBand()` が**いまの相場での評価額（`landAppraisal`）**で見る。
  固定の `baseValue` で切ると、相場が上がったときに区画が実際より安い帯に居座る
- **帯が空だったときに区画全体へ戻さないこと。** 隣の帯にずらす。
  全体に戻すと、自社が一等地を買い集めたあと
  「特大の帯を引いたのに小口の区画が出てくる」状態になり、
  長く遊ぶほど大型案件が出なくなる

## 社長と役員

**社長はプレイヤー本人である。** 架空の社員を社長の席に座らせないこと。

- `g.company.ceo = { name, age, since }`。創業時にタイトル画面で名前を入れる
- `RANKS` の `CEO_RANK`（等級7）は社長の席。`makeStaff()` は `TOP_STAFF_RANK` で頭打ちにしており、
  旧セーブで座っていた社員は `migrate()` が取締役に下ろす
- 社長の役員報酬は `hrPolicy.rankPay[7]`。`personnelCost()` に足してある
  （社員ではないので、社員の給与合計だけでは漏れる）
- 社員の平均年収（`playerHR`）には社長を含めない。有価証券報告書と同じ扱いである

**執行役員・取締役は自動で昇格しない。** `RANKS` の `appoint: true` がその印で、
`hr.js` の定期昇格ループはこの等級を飛ばす。任命は `src/sim/officers.js`。

- 任命した役員には**管掌部門**を持たせる。`oversightOf()` が部門ごとの押し上げを返し、
  `orgPower()` が質と処理能力に掛ける
- 同じ部門を複数の役員が見ても、いちばん強い1人ぶんしか効かない
- 能力が目安（`minAbility`）に届かない人物を引き上げると、実力のある社員の士気が下がる

役職名は `hrPolicy.rankNames` で改称できる。
**表示は必ず `rankName(g, id)` を通すこと。** `RANKS[x].name` を直接出すと改称が反映されない。

## 中期経営計画

`src/sim/midplan.js`。3年または5年の数値目標を最大4つ掲げる。

- 目標は `PLAN_METRICS`（売上高・営業利益・営業利益率・ROE・賃貸NOI・保有物件数・
  累計引渡戸数・保有区画数・ブランド・自己資本比率・従業員数）から選ぶ
- 公表で士気が上がり、株価に期待が乗る（`planPremium()` が `sharePrice()` に掛かる）
- 期限は四半期末に `stepPlan()` が判定する。達成でブランド+、未達で−、取り下げも−
- 背伸びの度合い（`ambitionOf`）が見返りと傷の両方を増やす

**指標を足すときは `get(g)` が必ず数値を返すようにすること。**
`g.finance.bs` は決算前は無いので、参照する指標はガードを入れる。

## 就職先ランキング

`src/data/employers.js`（架空の企業40社・11業界）と `src/sim/jobrank.js`。

- 人気度 … 素の知名度＋年収の魅力＋直近の勢い。デベロッパー8社は `rivalId` で競合と連動する
- 入社難易度 … 応募倍率（エントリー数 ÷ 採用予定数 × 選考の厳しさ）。
  **人気だけで決めないこと。** 採用数が10倍違えば倍率はまるで違う
- 自社の行は `employerAppeal()` のスコアから作る。
  **`jobrank.js` から `recruit.js` を読まないこと**（相互参照になる）。スコアは引数で受け取る
- ランキングの効き目は `rivalPull()`。同業他社の人気が自社より高いほど内定辞退が増える

## 都市と地盤

地図には2つの市がある。`src/data/city.js` の `CITIES` と、各地区の `city` で紐づく。

| 市 | 地区 | 性格 |
| --- | --- | --- |
| 湊都市 | T B A K N I S J F M E W P Z V Q | 地価も賃料も高い。キャップレートは低い |
| 鶴見野市 | Y H R G L C | 地価は安く、キャップレートが高い。開発利回りで稼ぐ |

- 鶴見野市の用地は `unlocked(g, 'city2')` まで売却情報に出てこない（`citiesOpen()`）
- 競合は湊都市の会社なので、鶴見野では入札意欲が 0.42 倍になる（`away`）
- 画面は `R.focusCity(cityId)` / `R.fit(cityId)` で街ごとに寄せる。
  **地図全体を映そうとしないこと。** 海ばかりになって街が豆粒になる

**地盤**（`g.company.home` と各社の `home`）は `src/sim/company.js` の `HOME` に集約している。
分譲単価・募集賃料・稼働率・提案コンペの評価点・売却情報の回り方に効く。
`info` は**割合**であって倍率ではない。
以前ここを `score` の掛け算にしていたら、地盤の区画が帯の上位を独占して
他の地区がまったく出てこなくなった。

## 売上に応じた解禁

`src/sim/company.js` の `TIERS`。直近4四半期の売上高で段階が決まり、
一度届いた解禁は `g.unlocked` に積まれて二度と外れない。

| 売上高 | 段階 | 解禁 |
| --- | --- | --- |
| — | 新興デベロッパー | 用地・開発・分譲 |
| 300億 | 中堅デベロッパー | 自社ブランド |
| 900億 | 準大手 | 子会社の設立 |
| 2,400億 | 大手 | 企業買収 |
| 6,000億 | 業界大手 | 公共案件 |
| 1兆4,000億 | 総合デベロッパー | 鶴見野市への進出 |

- 画面側は `unlocked(g, key)` で出し分け、`lockCard()` で「あと何億円か」を必ず見せる
- `main.js` の操作ハンドラにも同じ判定を入れる（画面だけの出し分けは素通りする）
- **古いセーブから機能を取り上げないこと。** `grantExisting()` が
  すでに使っている機能（ブランドを持っている、子会社がある…）を解禁済みに倒す

## 公共案件

`src/sim/land.js` の `PUBLIC_PROGRAMS`。`kind: 'public'` の売却情報として出る。

- 地価は相場の 5〜7割。そのかわり用途とグレード下限が指定され、
  公共貢献施設のぶん建設費が 3〜8.5% 増える（`applyProgram()`）
- 審査は企画7割・価格3割。公募価格を下回る提示はできない
- 条件は落札時に `cell.program` として区画に残る。
  `canStart(g, cell, use, grade)` が用途とグレードを見て着工を止める
- プログラムを足したら `fit` に入れる地区を必ず確認する。
  合わない地区に出すと、誰も応札しないまま流れ続ける

## 労働時間と残業

`src/sim/workload.js`。部署ごとに「仕事量」と「処理能力」を出し、
その比（負荷）が1.00を超えたぶんを残業時間に変える。

```
over = load - 1
残業 = clamp(pow(over, 0.75) * 55 * 人数による割増, 0, 140)   // 時間/月
```

- 仕事量の元になるのは、案件数・保有物件数・延床・借入残高など**実際の業務の重さ**である。
  適当な係数で置かないこと。155名・14案件・21物件・借入1.1兆円の状態で
  全社平均 8h/月、いちばん重い財務で 50h/月 前後になるよう校正してある
- 子会社と買収先は仕事を引き受ける（`relief`）。建設子会社があれば建設部門の仕事量が2割減る。
  **`effect` のキーを足したら `relief` にも通すか決めること**
- `BASE_HOURS = 160` が所定労働。`OVERTIME_DANGER = 60` で士気と離職に効きはじめ、
  `OVERTIME_LIMIT = 80` を超えると開示パネルで赤くなる
- 残業代は `OT_PREMIUM = 1.25` を掛けて `personnelCost()` に入れてある。
  **残業を増やしても人件費が増えないようにしないこと。** 只働きになる
- 働き方への投資は `WORK_PROGRAMS`（`hrPolicy.work`）。導入すると処理能力が上がるかわりに
  四半期ごとの固定費が増える
- 手元で見るときは `window.__wl()`（`main.js`）。`window.G` / `window.R` と同じデバッグ用の口である

## エンゲージメントサーベイ

`src/sim/survey.js`。毎年1回、`g.weekOfYear === 39`（10月）に実施する。

- 柱は `SURVEY_AXES` の6つで、重みの合計は 1.00。
  **重みを足し引きしたら合計を1.00に戻すこと**（スコアが100点満点でなくなる）
- `surveyNow(g)` はいつ呼んでも計算できる。画面のプレビューはこれを使い、
  記録に残すのは `runSurvey()` のときだけ
- 自由記述（`voices()`）は**1種類につき2件まで**に間引く。
  間引かないと同じ文面が10件並ぶ

## 表彰

`src/sim/awards.js`。四半期ごとに審査し、竣工2年以内の物件が対象。1物件1賞まで。

- 評点は `meritOf()`。グレード（6/34/58）＋規模＋階数＋立地＋ブランド＋複合用途。
  規模は `(log10(gfa) - 3.4) * 30` なので、**1万坪あたりから効きはじめる**。
  標準グレードの3,000坪はどの賞の候補にもならないのが狙い
- 受賞の効き目は `awardAppeal()`。**直近5年ぶんしか効かない**（最大 +0.10）。
  昔の受賞が永久に効くと、一度当てただけで採用が楽になりすぎる

## 格付けとIR

`src/sim/ir.js`。格付けと見通し（`OUTLOOKS`）、決算説明会の質疑を持つ。

**`outlookFactors()` から `kpis()` を呼ばないこと。**
`kpis` → `effectiveRate` → `outlookSpread` → `outlookOf` → `outlookFactors` と一周して
無限再帰（`Maximum call stack size exceeded`）になる。
自己資本比率が要るときは `buildBS(g)` から直接計算する。`questionsFor()` も同じ。

- 見通しは借入金利に乗る（`effectiveRate` に `outlookSpread`）。
  negative で +0.22%、positive で −0.15%
- 決算説明会の受け答えは `g.company.irTrust` と `irPrice` を動かし、
  `irPremium()` が株価に掛かる（0.85〜1.15倍）。`decayIr()` で毎週ゆるく戻る
- 質問は `questionsFor()` が状況（赤字・減収・借入超過・中計進捗・平時）から最大3問選ぶ

## 災害と鉄道

`src/sim/cityevents.js`。

- 地区ごとの `HAZARD` に地震・風水害・浸水の起きやすさを持たせている。
  **地区を足したら `HAZARD` にも足すこと**（無いと被害計算で落ちる）
- 被害の大きさは物件の `seismicOf(a)`。築12年を過ぎると年 0.012 ずつ落ち、下限 0.55。
  `RETROFIT` で耐震改修すると戻る（工事中は `sales.js` で稼働率が 0.55 倍になる）
- 台風・浸水・雪は `months` で季節を絞ってある。**通年で起こさないこと**
- 鉄道は `RAIL_PLANS`。発表 → 建設（4〜7年）→ 開業と進む。
  建設中から `railMul()` の期待が `landAppraisal()` に乗り、開業で `lift` が満額になる。
  **開業してから初めて上げるようにしないこと。** 実際の地価は計画段階から動く

## 社内公募と抜擢人事

`src/sim/talent.js`。

- 公募に手を挙げるのは「その仕事に向いている」か「いまの部署がつらい」人。
  残業時間（`s.otHours`）も押し出しの材料に入っている
- 抜擢は等級を2つ飛ばす。役員等級（`appoint: true`）には抜擢で上がれない
- **失敗したときの罰を本人だけにしないこと。** 飛び越された側（等級が間にあって
  実力が上の社員）の士気も落ちる。これが無いと抜擢を連打するのが最適手になる

## 開示・統合報告パネル

`src/ui/panelDisclosure.js`。有価証券報告書に近い見せ方で6タブ。
従業員の状況／エンゲージメント／格付け／受賞歴／リスクと街の変化／沿革。

数字はすべて他所の関数から引いてくる。**このパネルの中で計算しないこと。**
画面と本体で値が食い違う。

## 人口と世帯数

`src/sim/population.js`。地区ごとに人口と世帯数を持ち、四半期に1回だけ動かす。

- 動かす力は3つ。都市の趨勢（湊都市は微増、鶴見野市は微減）、
  地区の引力（駅力・新線・住宅の厚み・地価の安さ）、混雑
- 地図は街の縮図なので `POP_SCALE` で引き伸ばす。
  掛けないと「人口60万の中核市」が2万7千人の町になる
- `residentialWeight()` は住宅系の適合を**2乗**する。
  線形のままだと、住宅適合が中くらいのビジネス街にも住宅地の半分の人口が住むことになる
- **人口を賃料や分譲単価に直接掛けないこと。** `demandMul()` は 0.88〜1.14 に抑えてあり、
  効かせるのは稼働率（`sales.js`）と成約速度（`project.js` の `contractSpeed`）だけである

## 株主総会

`src/sim/meeting.js`。上場していれば毎年6月（`MEETING_WEEK = 24`）に開く。

- 賛成率を決めるのは**経営の成績**（`scoreOf`）。ROE・株価・配当性向・中計の進捗・IR信頼度。
  **議案ごとの好き嫌いだけで決めないこと。** 成績が悪ければ会社提案でも危うくなる
- 株主は6種類（`HOLDERS`）。`loyalty` が低い株主ほど成績に敏感に反応する
- 成績不振が続くと物言う株主（`activistShare`）が買い増し、株主提案が出やすくなる。
  2期続けて不振だと代表取締役の解任動議までかかる
- **`scoreOf()` から `kpis()` を呼ばないこと。** ir.js と同じで一周して無限再帰になる
- 配当は `finance.js` の `closeQuarter()` で**第4四半期だけ**払う

## 労働組合と春闘

`src/sim/union.js`。社員が `UNION_MIN_STAFF`（60名）を超え、士気が低いか残業が長いと結成される。

- 毎年2月（`DEMAND_WEEK = 6`）に要求が出る。要求の水準は世間相場との差・利益率・
  物価・残業時間から決まる
- **飲む／蹴るの二択にしないこと。** 回答の水準をプレイヤーが決め、
  要求との比（`acceptanceOf`）で妥結・不満含みの決着・決裂が分かれる
- 決裂すると士気と定着が大きく落ち、ブランドも下がる。回答しないまま3月半ばを過ぎると
  ゼロ回答として扱われる

## 画面に出すタイミング

**決算週以外のイベントは `afterReport()` を通すこと。**
以前 `presentResults()` が決算報告を閉じたときにしか `afterReport()` を呼んでおらず、
決算週に当たらない株主総会（6月）と春季交渉（2月）が一度も開かれなかった。

`openModal(title, html, buttons)` のボタンは `{ label, cls, onClick, close }` である。
**`kind` や `act` は見ていない。** 書き間違えるとボタンが押せるのに何も起きない。

## 描画を重くしないための決めごと

**スマートフォンが熱くなる原因は、ほぼ次の2つである。**

1. **覆われているのにキャンバスを描き続けること。**
   パネルやダイアログの `backdrop-filter`（すりガラス）は、
   下のキャンバスが動くかぎり毎フレームぼかしを焼き直す。
   `main.js` の `cityHidden()` で、隠れている間は1枚も描かない
2. **地面を1枚ずつ描くこと。**
   `groundLayer()` は**画面のまわりだけ**をオフスクリーンに焼く。
   マップ全体を焼こうとすると、拡大時に上限（6e6px）を超えて
   毎フレーム1000枚のタイルを描くことになり、1フレーム 20ms を超える。
   焼いた範囲は `cov`（面倒を見ると決めたワールド座標の矩形）で持ち、
   画面がその中にあるかぎり焼き直さない。
   **`cov` ではなく画像の大きさで判定しないこと。**
   マップの端に寄せたときに毎フレーム焼き直しになる

そのほか：

- コマ数は `FPS_IDLE`（スマホ24／PC30）。操作中だけ60に上げる（`wake()`）
- 空のグラデーションは時間帯と画面サイズでしか変わらないので使い回す
- 全画面のシートになるパネルと、ヘッダー・下部タブバーの
  `backdrop-filter` は狭い画面では切ってある（不透明なので見た目は変わらない）

計測は `scratchpad` の `zperf.mjs`（ズーム段階ごとの1回あたりの描画時間）と
`heat.mjs`（パネルを開いた状態で描画が止まるか）で行う。

## 地区を追加するとき

地区の定義は `src/data/city.js` の `DISTRICTS` に集約している。
1地区あたり次をすべて埋めること。1つ抜けると新規ゲームの生成で落ちる。

- `city` … どちらの市か（`minato` / `tsurumino`）
- `lotSize` … 区画面積の基準（坪）
- `height` … 既存の建物の階数レンジ
- `usePool` … 既存の建物の用途の出方
- `rentLogi` … 物流賃料。**省略するとオフィス賃料の6割が代用され、都心ほど物流が有利になる**
- `fit` に `rental` を含めること
- `src/sim/cityevents.js` の `HAZARD` にも同じキーで足すこと（災害の被害計算が参照する）

地図を広げるときは `MAP_ROWS` の**右側と下側にだけ**足す。
区画のIDは `p{x}_{y}` なので、既存の区画の座標さえ動かなければ古いセーブはそのまま引き継げる
（`remapCells()` がIDで突き合わせる）。`MAP_W` と `MAP_H` は同じ値にしておくこと
（`rotate()` が両方を使う）。

数値を決めたら、事業収支を必ず確認する。
地区ごとに「本命の用途」が1つ決まり、不適な用途がはっきり赤字になっているのが狙い。

```bash
node scripts/check-districts.mjs   # 全地区×全用途の事業利益率を一覧する
node scripts/check-lifecycle.mjs   # 着工から完売までの金の流れを突き合わせる
```

## 描画の性能

- 地面（道路・公園・水面・区画の下地）は毎フレーム1000枚以上のタイルになるので、
  `groundLayer()` で1枚のオフスクリーンに焼いて貼っている
- 焼き直しの判定は `groundSig()`（建物の有無・更地・着工中、所有者表示のときは所有者）。
  地面の見た目に関わる状態を増やしたら、この指紋にも足すこと
- `drawOrder()` は回転ごとにキャッシュする。セーブを読み込むと区画が入れ替わるので
  `invalidate()` で捨てている
- 縮小時（zoom < 0.40）は建物の影を省く

## スマートフォン対応

- CSS の分岐は `styles/main.css` 末尾の `@media(max-width:820px)`。
  レイアウト寸法は `--topH`（ヘッダー高）と `--botH`（画面下部の高さ）の2変数で決まる
- 狭い画面では、左の縦タブが画面下のタブバーに、パネルが全画面のシートになる
- 画面が狭いかどうかの判定は `src/main.js` の `IS_SMALL`。CSS の 820px と合わせること
- 指の操作は `bindInput()` 内。1本指＝カメラ移動、2本指＝ピンチで段階ズーム。
  `#city` には `touch-action:none` が必要（外すとブラウザ側のスクロールに取られる）
- 入力欄の文字サイズは16px。これを下回ると iOS がフォーカス時に勝手にズームする
- `manifest.webmanifest` と `sw.js` により、ホーム画面に追加すると全画面で起動し、
  一度読み込めば電波がなくても遊べる

## 動作確認

ローカルで開くときは HTTP を使う（ES Modules は `file://` で動かない）。

```bash
python3 -m http.server 8000   # → http://localhost:8000/index.html
```

単一ファイル版は `file://` で直接開いて確認できる。
